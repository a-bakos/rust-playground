// open csv
// parse csv rows
// sort draw numbers

#[derive(Debug)]
struct Draw {
    id: String, // draw number
    date: String,
    balls: Vec<u8>,
    set: u8,
    machine: String,
}

fn main() {
    println!("Hello, world!");
}
